name: whu_calendar
description: WHU academic calendar data and Flutter utilities — multi-language (TS + Dart) support.
version: 0.3.1
repository: https://github.com/ClosedWHU/WHU-Calendar
issue_tracker: https://github.com/ClosedWHU/WHU-Calendar/issues

environment:
  sdk: ">=3.9.0 <4.0.0"
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  assets:
    - assets/data/
