name: whu_calendar
description: WHU academic calendar data and Flutter utilities — multi-language (TS + Dart) support.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.9.0 <4.0.0"
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  assets:
    - assets/data/
